[package]
name = "proc-pbac-macro"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Attribute macro that injects a policy-based access-control check into an async method"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0.89"
quote = "1.0.37"
syn = { version = "2.0.85", features = ["full"] }
