[package]
name = "geo-bindgen-macro"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Derive macro that generates From/Into bindings between an idiomatic geometry type and its native FFI representation"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0.89"
quote = "1.0.37"
syn = { version = "2.0.85", features = ["full"] }
